galaxy_info:
  author: Adnan Hodzic
  description: Deploy & run Docker Compose project for WordPress instance (3 separate containers running WordPress PHP7 FPM, Nginx and MariaDB)

  license: license (GPLv3)

  min_ansible_version: 2.0

  platforms:
  - name: Ubuntu
    versions:
    - all

  galaxy_tags:
    - development
    - system
    - docker
    - compose
    - ubuntu
    - wordpress
    - nginx
    - mariadb
    - mysql
    - php7
    - fpm
    - container
    - dockerized
    - containzerized
    - project
    - ec2
    - aws
    - automation

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
